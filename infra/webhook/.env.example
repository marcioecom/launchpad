# HMAC secrets por projeto. Um por projeto que tem deploy automatizado.
# Gere com: openssl rand -hex 32
# Mesma string deve ser configurada como secret no repo correspondente do GitHub.
#
# NUNCA commite este arquivo (já no .gitignore).

LUPA_DEPLOY_SECRET=trocar-por-openssl-rand-hex-32
AI_CHATBOT_DEPLOY_SECRET=trocar-por-openssl-rand-hex-32
